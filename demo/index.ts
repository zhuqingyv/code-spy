const a = () => {
  /**
   * @xhs spy
   * @type test
   * @name base render
   * @example spy('spy-name', () => {
   *   return true;
   * })
  */

  /**
   * @xhs spy
   * @type test
   * @name base render2
   * @example spy('spy-name2', () => {
   *   return true;
   * })
  */
};